<Window x:Class="ShareQ.App.Views.QrCodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="QR code"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="#1E1E1E"
        Foreground="White"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="True">
    <StackPanel Margin="20">
        <TextBlock Text="Scan to open" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,12" />
        <Border Padding="8" Background="White" CornerRadius="4">
            <Image x:Name="QrImage" Width="280" Height="280" Stretch="Uniform" />
        </Border>
        <TextBlock x:Name="UrlText" Foreground="#BBB" FontSize="11" FontFamily="Consolas"
                   HorizontalAlignment="Center" TextTrimming="CharacterEllipsis"
                   MaxWidth="280" Margin="0,8,0,0" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
            <ui:Button x:Name="CopyButton" Content="Copy URL" Width="100" Margin="0,0,8,0" />
            <ui:Button x:Name="CloseButton" Content="Close" Width="100" />
        </StackPanel>
    </StackPanel>
</Window>
