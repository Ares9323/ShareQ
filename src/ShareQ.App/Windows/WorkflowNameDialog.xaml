<Window x:Class="ShareQ.App.Windows.WorkflowNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Workflow name"
        Width="380" Height="170"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="#1E1E1E"
        Foreground="White"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleText" Grid.Row="0" Text="Workflow name" FontSize="14" FontWeight="SemiBold" />
        <TextBlock x:Name="HintText" Grid.Row="1" Foreground="#888" FontSize="11" TextWrapping="Wrap"
                   Margin="0,4,0,12"
                   Text="Choose a name for this workflow. Built-in workflows can also be renamed; the change is local and survives across restarts." />
        <ui:TextBox x:Name="NameTextBox" Grid.Row="2" VerticalAlignment="Top" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <ui:Button x:Name="OkButton"     Content="OK"     Width="80" Margin="0,0,8,0" />
            <ui:Button x:Name="CancelButton" Content="Cancel" Width="80" />
        </StackPanel>
    </Grid>
</Window>
