namespace AresToys.AI;

/// <summary>Empty marker class — used by Microsoft.Extensions logging / DI to identify
/// this assembly without picking a real production type. ONNX Runtime dependencies will
/// be added here later when the background-removal feature lands; for now this project
/// only hosts the image-trace abstractions (potrace shell-out).</summary>
public static class AssemblyMarker;
